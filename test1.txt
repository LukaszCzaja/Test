TCos tworzenieCos()
{
	if (cos == NULL)
	{
		cos = new TCos();
	}	
}

int dodawanie()
{
	int result = COS.A * COS.B
	return (double) result;
}

int odejmowanie()
{
	int wynik = 0;
	for (int i = 0; i < COS.Ilosc; i++)
	{
		wynik = COS.B - COS.A;
	}
	return wynik;
}